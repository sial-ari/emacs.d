#!/usr/bin/env ruby

require 'optparse'

options = {}
OptionParser.new do |opt|
  opt.on('--amount NUM CURR') { |o| options[:amount] = o }
  opt.on('--tip NUM CURR') { |o| options[:tip] = o }
end.parse!

puts options
